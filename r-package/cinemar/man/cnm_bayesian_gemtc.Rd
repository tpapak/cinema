% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian.R
\name{cnm_bayesian_gemtc}
\alias{cnm_bayesian_gemtc}
\title{Extract a Bayesian block directly from gemtc objects}
\usage{
cnm_bayesian_gemtc(
  result,
  nodesplit = NULL,
  ranks = NULL,
  data_points = NULL,
  flip = FALSE
)
}
\arguments{
\item{result}{A `mtc.result` from `gemtc::mtc.run()`.}

\item{nodesplit}{Optional `mtc.nodesplit` result from `gemtc::mtc.nodesplit()`.}

\item{ranks}{Optional rank-probability matrix from
`gemtc::rank.probability(result)` (treatments x ranks).}

\item{data_points}{Optional integer number of data points for DIC; inferred
from the network when omitted.}

\item{flip}{Logical; invert the effect orientation (default `FALSE`).}
}
\value{
A list shaped as the schema `bayesianResults` object.
}
\description{
Convenience wrapper that pulls posterior summaries out of a fitted
\pkg{gemtc} model (`gemtc::mtc.run` result) and optional nodesplit / ranking
objects, then hands them to [cnm_bayesian_block()].
}
\details{
Orientation: gemtc's basic parameters `d.A.B` are the effect of B relative to
A. CINeMA reports `"A:B"` as A relative to B, so effects are negated to match
(set `flip = TRUE` if a sanity check against the frequentist league table
shows the table transposed for your gemtc version).
}
