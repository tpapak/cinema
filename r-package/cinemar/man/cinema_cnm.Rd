% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnm.R
\name{cinema_cnm}
\alias{cinema_cnm}
\title{Build a CINeMA v3 .cnm object from NMA data}
\usage{
cinema_cnm(
  data,
  type,
  sm,
  model = "random",
  bayesian = NULL,
  title = "project",
  outcome = "",
  dataset_type = "continuous",
  ...
)
}
\arguments{
\item{data}{NMA data frame (see [cnm_frequentist_block()] for the expected
columns per `type`).}

\item{type}{One of `"long_binary"`, `"long_continuous"`, `"iv"`.}

\item{sm}{Effect measure, e.g. `"OR"`, `"MD"`.}

\item{model}{`"random"` (default) or `"fixed"`.}

\item{bayesian}{Optional Bayesian results. Accepts a `gemtc` `mtc.result`
(extracted via [cnm_bayesian_gemtc()]), a data frame of comparisons
(passed to [cnm_bayesian_block()]), or an already-built Bayesian block list.}

\item{title}{Project title.}

\item{outcome}{Optional outcome label.}

\item{dataset_type}{For `type = "iv"`: `"binary"` or `"continuous"`.}

\item{...}{Passed to [cnm_bayesian_gemtc()] when `bayesian` is a gemtc result
(e.g. `nodesplit`, `ranks`, `flip`).}
}
\value{
A nested list matching cinema_schema_v3.json.
}
\description{
One-call entry point. Runs \pkg{netmeta} for the structural block CINeMA
needs, and — when a Bayesian fit is supplied — attaches the Bayesian
posterior results as the primary estimates.
}
